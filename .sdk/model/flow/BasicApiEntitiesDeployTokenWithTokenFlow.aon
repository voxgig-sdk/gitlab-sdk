# BasicApiEntitiesDeployTokenWithTokenFlow

main: kit: flow: BasicApiEntitiesDeployTokenWithTokenFlow:
{
  entity: "api_entities_deploy_token_with_token",
  kind: "basic",
  name: "BasicApiEntitiesDeployTokenWithTokenFlow",
  step: [
    {
      input: {
        ref: "api_entities_deploy_token_with_token_ref01"
      }

      match: {
        project_id: "project01"
      }

      op: "create"
    }
  ],
  "key$": "BasicApiEntitiesDeployTokenWithTokenFlow"
}