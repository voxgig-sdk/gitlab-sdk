// Completeness gate over every TypeScript example in this SDK's docs.
//
// Covers all THREE documentation files — the repo root README.md, this
// target's README.md, and its REFERENCE.md. Every fenced ```ts block in
// every one is put through the same pipeline:
//
//  (1) TYPE-CHECK — every block is compiled with the repo's local
//      `tsc --noEmit` against the real SDK types. A block that misuses the
//      SDK types (e.g. `.data` on a `list()` array, or a string id where a
//      number is required) is a compile error.
//  (2) EXECUTE — every RUNNABLE block (one that constructs the SDK or
//      drives `client.`) is rewritten to seeded test mode, transpiled to
//      JS, and run offline. A programming error (undefined method,
//      wrong-arg-count, bad symbol) fails the test; only an expected domain
//      error (a 404 / "Not found" for an unseeded id) is tolerated.
//  (3) COMPLETENESS — every block is partitioned into exactly one of
//      { executed, typechecked-only, illustration } and the counts must sum
//      to the total. A block that does NOT type-check is allowed ONLY if it
//      matches the NARROW illustration allowlist (a bare method signature
//      `?:`, a `/* placeholder */` value, or a type-SHAPE block). Any other
//      non-type-checking block FAILS the test, quoting the block — it is
//      never silently skipped.
//
// Generated by @voxgig/sdkgen — do not edit by hand.

import { describe, it } from 'node:test'
import assert from 'node:assert'
import * as Fs from 'node:fs'
import * as Path from 'node:path'
import { createRequire } from 'node:module'
import { spawnSync } from 'node:child_process'

import { GitlabSDK } from '..'


const SDK_NAME = 'GitlabSDK'

// A fixture for every entity, so list()/load() resolve offline with no
// network. Snippet client construction is rewritten to seed this.
const TEST_SEED = {"entity":{"access_request":{"test01":{"id":"test01"}},"alert_management":{"test01":{"id":"test01"}},"api_entities_access_requester":{"test01":{"id":"test01"}},"api_entities_appearance":{"test01":{"id":"test01"}},"api_entities_application":{"test01":{"id":"test01"}},"api_entities_application_statistic":{"test01":{"id":"test01"}},"api_entities_application_with_secret":{"test01":{"id":"test01"}},"api_entities_avatar":{"test01":{"id":"test01"}},"api_entities_award_emoji":{"test01":{"id":"test01"}},"api_entities_badge":{"test01":{"id":"test01"}},"api_entities_basic_badge_detail":{"test01":{"id":"test01"}},"api_entities_basic_group_detail":{"test01":{"id":"test01"}},"api_entities_basic_project_detail":{"test01":{"id":"test01"}},"api_entities_basic_ref":{"test01":{"id":"test01"}},"api_entities_basic_success":{"test01":{"id":"test01"}},"api_entities_batched_background_migration":{"test01":{"id":"test01"}},"api_entities_branch":{"test01":{"id":"test01"}},"api_entities_bulk_import":{"test01":{"id":"test01"}},"api_entities_bulk_imports_entity_failure":{"test01":{"id":"test01"}},"api_entities_bulk_imports_export_status":{"test01":{"id":"test01"}},"api_entities_changelog":{"test01":{"id":"test01"}},"api_entities_ci_bridge":{"test01":{"id":"test01"}},"api_entities_ci_catalog_resources_version":{"test01":{"id":"test01"}},"api_entities_ci_job":{"test01":{"id":"test01"}},"api_entities_ci_job_basic":{"test01":{"id":"test01"}},"api_entities_ci_job_basic_with_project":{"test01":{"id":"test01"}},"api_entities_ci_lint_result":{"test01":{"id":"test01"}},"api_entities_ci_pipeline":{"test01":{"id":"test01"}},"api_entities_ci_pipeline_basic":{"test01":{"id":"test01"}},"api_entities_ci_pipeline_schedule":{"test01":{"id":"test01"}},"api_entities_ci_pipeline_schedule_detail":{"test01":{"id":"test01"}},"api_entities_ci_reset_token_result":{"test01":{"id":"test01"}},"api_entities_ci_resource_group":{"test01":{"id":"test01"}},"api_entities_ci_runner":{"test01":{"id":"test01"}},"api_entities_ci_runner_detail":{"test01":{"id":"test01"}},"api_entities_ci_runner_manager":{"test01":{"id":"test01"}},"api_entities_ci_runner_registration_detail":{"test01":{"id":"test01"}},"api_entities_ci_secure_file":{"test01":{"id":"test01"}},"api_entities_ci_variable":{"test01":{"id":"test01"}},"api_entities_cluster":{"test01":{"id":"test01"}},"api_entities_cluster_group":{"test01":{"id":"test01"}},"api_entities_cluster_project":{"test01":{"id":"test01"}},"api_entities_clusters_agent":{"test01":{"id":"test01"}},"api_entities_clusters_agent_token":{"test01":{"id":"test01"}},"api_entities_clusters_agent_token_basic":{"test01":{"id":"test01"}},"api_entities_clusters_agent_token_with_token":{"test01":{"id":"test01"}},"api_entities_commit":{"test01":{"id":"test01"}},"api_entities_commit_detail":{"test01":{"id":"test01"}},"api_entities_commit_note":{"test01":{"id":"test01"}},"api_entities_commit_sequence":{"test01":{"id":"test01"}},"api_entities_commit_signature":{"test01":{"id":"test01"}},"api_entities_commit_status":{"test01":{"id":"test01"}},"api_entities_compare":{"test01":{"id":"test01"}},"api_entities_container_registry_repository":{"test01":{"id":"test01"}},"api_entities_container_registry_tag":{"test01":{"id":"test01"}},"api_entities_container_registry_tag_detail":{"test01":{"id":"test01"}},"api_entities_contributor":{"test01":{"id":"test01"}},"api_entities_deploy_key":{"test01":{"id":"test01"}},"api_entities_deploy_keys_project":{"test01":{"id":"test01"}},"api_entities_deploy_token":{"test01":{"id":"test01"}},"api_entities_deploy_token_with_token":{"test01":{"id":"test01"}},"api_entities_deployment":{"test01":{"id":"test01"}},"api_entities_deployment_extended":{"test01":{"id":"test01"}},"api_entities_deployments_approval":{"test01":{"id":"test01"}},"api_entities_dictionary_table":{"test01":{"id":"test01"}},"api_entities_diff":{"test01":{"id":"test01"}},"api_entities_discovered_cluster":{"test01":{"id":"test01"}},"api_entities_draft_note":{"test01":{"id":"test01"}},"api_entities_environment":{"test01":{"id":"test01"}},"api_entities_error_tracking_client_key":{"test01":{"id":"test01"}},"api_entities_error_tracking_project_setting":{"test01":{"id":"test01"}},"api_entities_event":{"test01":{"id":"test01"}},"api_entities_feature":{"test01":{"id":"test01"}},"api_entities_feature_definition":{"test01":{"id":"test01"}},"api_entities_feature_flag":{"test01":{"id":"test01"}},"api_entities_feature_flag_user_list":{"test01":{"id":"test01"}},"api_entities_freeze_period":{"test01":{"id":"test01"}},"api_entities_gitlab_subscription":{"test01":{"id":"test01"}},"api_entities_go_module_version":{"test01":{"id":"test01"}},"api_entities_group":{"test01":{"id":"test01"}},"api_entities_group_detail":{"test01":{"id":"test01"}},"api_entities_hook":{"test01":{"id":"test01"}},"api_entities_integration":{"test01":{"id":"test01"}},"api_entities_integration_basic":{"test01":{"id":"test01"}},"api_entities_invitation":{"test01":{"id":"test01"}},"api_entities_issuable_time_stat":{"test01":{"id":"test01"}},"api_entities_issue":{"test01":{"id":"test01"}},"api_entities_issue_link":{"test01":{"id":"test01"}},"api_entities_license":{"test01":{"id":"test01"}},"api_entities_markdown":{"test01":{"id":"test01"}},"api_entities_markdown_upload_admin":{"test01":{"id":"test01"}},"api_entities_member":{"test01":{"id":"test01"}},"api_entities_merge":{"test01":{"id":"test01"}},"api_entities_merge_request_approval":{"test01":{"id":"test01"}},"api_entities_merge_request_basic":{"test01":{"id":"test01"}},"api_entities_merge_request_change":{"test01":{"id":"test01"}},"api_entities_merge_request_diff":{"test01":{"id":"test01"}},"api_entities_merge_request_diff_full":{"test01":{"id":"test01"}},"api_entities_merge_request_reviewer":{"test01":{"id":"test01"}},"api_entities_metric_image":{"test01":{"id":"test01"}},"api_entities_mr_note":{"test01":{"id":"test01"}},"api_entities_namespace":{"test01":{"id":"test01"}},"api_entities_namespace_existence":{"test01":{"id":"test01"}},"api_entities_namespaces_storage_limit_exclusion":{"test01":{"id":"test01"}},"api_entities_npm_package":{"test01":{"id":"test01"}},"api_entities_npm_package_tag":{"test01":{"id":"test01"}},"api_entities_nuget_packages_version":{"test01":{"id":"test01"}},"api_entities_nuget_search_result":{"test01":{"id":"test01"}},"api_entities_nuget_service_index":{"test01":{"id":"test01"}},"api_entities_organizations_organization":{"test01":{"id":"test01"}},"api_entities_package":{"test01":{"id":"test01"}},"api_entities_package_file":{"test01":{"id":"test01"}},"api_entities_package_pipeline":{"test01":{"id":"test01"}},"api_entities_packages_conan_files_list":{"test01":{"id":"test01"}},"api_entities_packages_conan_package_manifest":{"test01":{"id":"test01"}},"api_entities_packages_conan_package_revision":{"test01":{"id":"test01"}},"api_entities_packages_conan_package_snapshot":{"test01":{"id":"test01"}},"api_entities_packages_conan_recipe_manifest":{"test01":{"id":"test01"}},"api_entities_packages_conan_recipe_revision":{"test01":{"id":"test01"}},"api_entities_packages_conan_recipe_snapshot":{"test01":{"id":"test01"}},"api_entities_packages_conan_revision":{"test01":{"id":"test01"}},"api_entities_packages_conan_upload_url":{"test01":{"id":"test01"}},"api_entities_packages_debian_distribution":{"test01":{"id":"test01"}},"api_entities_pages_domain":{"test01":{"id":"test01"}},"api_entities_pages_domain_basic":{"test01":{"id":"test01"}},"api_entities_personal_access_token":{"test01":{"id":"test01"}},"api_entities_personal_access_token_with_last_used_ip":{"test01":{"id":"test01"}},"api_entities_personal_access_token_with_token":{"test01":{"id":"test01"}},"api_entities_personal_snippet":{"test01":{"id":"test01"}},"api_entities_plan_limit":{"test01":{"id":"test01"}},"api_entities_project":{"test01":{"id":"test01"}},"api_entities_project_daily_statistic":{"test01":{"id":"test01"}},"api_entities_project_export_status":{"test01":{"id":"test01"}},"api_entities_project_group_link":{"test01":{"id":"test01"}},"api_entities_project_hook":{"test01":{"id":"test01"}},"api_entities_project_import_status":{"test01":{"id":"test01"}},"api_entities_project_job_token_scope":{"test01":{"id":"test01"}},"api_entities_project_repository_storage":{"test01":{"id":"test01"}},"api_entities_project_snippet":{"test01":{"id":"test01"}},"api_entities_project_upload":{"test01":{"id":"test01"}},"api_entities_project_with_access":{"test01":{"id":"test01"}},"api_entities_projects_container_registry_protection_rule":{"test01":{"id":"test01"}},"api_entities_projects_packages_protection_rule":{"test01":{"id":"test01"}},"api_entities_projects_topic":{"test01":{"id":"test01"}},"api_entities_protected_branch":{"test01":{"id":"test01"}},"api_entities_protected_tag":{"test01":{"id":"test01"}},"api_entities_public_group_detail":{"test01":{"id":"test01"}},"api_entities_related_issue":{"test01":{"id":"test01"}},"api_entities_relation_import_tracker":{"test01":{"id":"test01"}},"api_entities_release":{"test01":{"id":"test01"}},"api_entities_releases_link":{"test01":{"id":"test01"}},"api_entities_remote_mirror":{"test01":{"id":"test01"}},"api_entities_repository_health":{"test01":{"id":"test01"}},"api_entities_resource_access_token_with_token":{"test01":{"id":"test01"}},"api_entities_resource_milestone_event":{"test01":{"id":"test01"}},"api_entities_snippet":{"test01":{"id":"test01"}},"api_entities_ssh_key_with_user":{"test01":{"id":"test01"}},"api_entities_suggestion":{"test01":{"id":"test01"}},"api_entities_system_broadcast_message":{"test01":{"id":"test01"}},"api_entities_tag":{"test01":{"id":"test01"}},"api_entities_tag_signature":{"test01":{"id":"test01"}},"api_entities_templates_list":{"test01":{"id":"test01"}},"api_entities_terraform_module_version":{"test01":{"id":"test01"}},"api_entities_tree_object":{"test01":{"id":"test01"}},"api_entities_trigger":{"test01":{"id":"test01"}},"api_entities_user_agent_detail":{"test01":{"id":"test01"}},"api_entities_user_count":{"test01":{"id":"test01"}},"api_entities_user_public":{"test01":{"id":"test01"}},"api_entities_user_with_admin":{"test01":{"id":"test01"}},"api_entities_wiki_attachment":{"test01":{"id":"test01"}},"api_entities_wiki_page":{"test01":{"id":"test01"}},"api_entities_wiki_page_basic":{"test01":{"id":"test01"}},"application":{"test01":{"id":"test01"}},"award_emoji":{"test01":{"id":"test01"}},"badge":{"test01":{"id":"test01"}},"branch":{"test01":{"id":"test01"}},"cargo_package":{"test01":{"id":"test01"}},"ci_variable":{"test01":{"id":"test01"}},"cluster":{"test01":{"id":"test01"}},"cluster_agent":{"test01":{"id":"test01"}},"composer":{"test01":{"id":"test01"}},"composer_package":{"test01":{"id":"test01"}},"conan":{"test01":{"id":"test01"}},"conan_package":{"test01":{"id":"test01"}},"container_registry":{"test01":{"id":"test01"}},"container_registry_event":{"test01":{"id":"test01"}},"custom_attribute":{"test01":{"id":"test01"}},"debian":{"test01":{"id":"test01"}},"debian_distribution":{"test01":{"id":"test01"}},"debian_package":{"test01":{"id":"test01"}},"dependency_proxy":{"test01":{"id":"test01"}},"deploy_key":{"test01":{"id":"test01"}},"deploy_token":{"test01":{"id":"test01"}},"deployment":{"test01":{"id":"test01"}},"ee_api_entities_approval_state":{"test01":{"id":"test01"}},"ee_api_entities_audit_event":{"test01":{"id":"test01"}},"ee_api_entities_billable_membership":{"test01":{"id":"test01"}},"ee_api_entities_geo_node_status":{"test01":{"id":"test01"}},"ee_api_entities_geo_pipeline_ref":{"test01":{"id":"test01"}},"ee_api_entities_issuable_metric_image":{"test01":{"id":"test01"}},"ee_api_entities_merge_request_approval_state":{"test01":{"id":"test01"}},"ee_api_entities_ssh_certificate":{"test01":{"id":"test01"}},"environment":{"test01":{"id":"test01"}},"error_tracking_client_key":{"test01":{"id":"test01"}},"feature":{"test01":{"id":"test01"}},"feature_flag":{"test01":{"id":"test01"}},"feature_flags_user_list":{"test01":{"id":"test01"}},"freeze_period":{"test01":{"id":"test01"}},"generic_package":{"test01":{"id":"test01"}},"geo":{"test01":{"id":"test01"}},"go_proxy":{"test01":{"id":"test01"}},"group":{"test01":{"id":"test01"}},"group_avatar":{"test01":{"id":"test01"}},"group_export":{"test01":{"id":"test01"}},"group_import":{"test01":{"id":"test01"}},"helm_package":{"test01":{"id":"test01"}},"hook":{"test01":{"id":"test01"}},"import":{"test01":{"id":"test01"}},"integration":{"test01":{"id":"test01"}},"invitation":{"test01":{"id":"test01"}},"issue_link":{"test01":{"id":"test01"}},"issues_statistic":{"test01":{"id":"test01"}},"job":{"test01":{"id":"test01"}},"maven_package":{"test01":{"id":"test01"}},"member":{"test01":{"id":"test01"}},"merge_request":{"test01":{"id":"test01"}},"metadata":{"test01":{"id":"test01"}},"migration":{"test01":{"id":"test01"}},"ml_model_registry":{"test01":{"id":"test01"}},"namespace":{"test01":{"id":"test01"}},"npm":{"test01":{"id":"test01"}},"npm_package":{"test01":{"id":"test01"}},"nuget":{"test01":{"id":"test01"}},"nuget_package":{"test01":{"id":"test01"}},"package_file":{"test01":{"id":"test01"}},"page":{"test01":{"id":"test01"}},"participant":{"test01":{"id":"test01"}},"personal_access_token":{"test01":{"id":"test01"}},"project":{"test01":{"id":"test01"}},"project_avatar":{"test01":{"id":"test01"}},"project_entity":{"test01":{"id":"test01"}},"project_export":{"test01":{"id":"test01"}},"project_hook":{"test01":{"id":"test01"}},"project_import":{"test01":{"id":"test01"}},"project_import_entity":{"test01":{"id":"test01"}},"project_package":{"test01":{"id":"test01"}},"project_snippet":{"test01":{"id":"test01"}},"projects_job_token_scope":{"test01":{"id":"test01"}},"protected_tag":{"test01":{"id":"test01"}},"pypi":{"test01":{"id":"test01"}},"pypi_package":{"test01":{"id":"test01"}},"release":{"test01":{"id":"test01"}},"release_link":{"test01":{"id":"test01"}},"remote_mirror":{"test01":{"id":"test01"}},"rpm":{"test01":{"id":"test01"}},"rpm_package":{"test01":{"id":"test01"}},"rubygem":{"test01":{"id":"test01"}},"rubygem_package":{"test01":{"id":"test01"}},"runner":{"test01":{"id":"test01"}},"search":{"test01":{"id":"test01"}},"secure_file":{"test01":{"id":"test01"}},"slack":{"test01":{"id":"test01"}},"snippet":{"test01":{"id":"test01"}},"starrer":{"test01":{"id":"test01"}},"system_hook":{"test01":{"id":"test01"}},"tag":{"test01":{"id":"test01"}},"terraform_registry":{"test01":{"id":"test01"}},"terraform_state":{"test01":{"id":"test01"}},"test_report":{"test01":{"id":"test01"}},"test_report_summary":{"test01":{"id":"test01"}},"topic":{"test01":{"id":"test01"}},"unleash_api":{"test01":{"id":"test01"}},"usage_data":{"test01":{"id":"test01"}},"user":{"test01":{"id":"test01"}},"web_commit":{"test01":{"id":"test01"}},"wiki":{"test01":{"id":"test01"}}}}
const SEED_ARG = JSON.stringify(TEST_SEED)
const SEEDED_CTOR = SDK_NAME + '.test(' + SEED_ARG + ')'


// The three docs this gate covers, resolved relative to dist-test/.
const DOCS: Array<{ label: string; key: string; path: string }> = [
  { label: 'root README.md', key: 'root_readme', path: Path.join(__dirname, '..', '..', 'README.md') },
  { label: 'ts/README.md', key: 'lang_readme', path: Path.join(__dirname, '..', 'README.md') },
  { label: 'ts/REFERENCE.md', key: 'reference', path: Path.join(__dirname, '..', 'REFERENCE.md') },
]


// Pull out the body of every ```ts fenced code block.
function extractTsBlocks(md: string): string[] {
  const blocks: string[] = []
  const re = /```ts\n([\s\S]*?)```/g
  let m: RegExpExecArray | null
  while ((m = re.exec(md)) !== null) {
    blocks.push(m[1])
  }
  return blocks
}


// Snippet import lines reference the published package name, which is not
// resolvable from inside the repo; the SDK symbol is supplied by the one
// module-level import we emit / inject instead.
function stripImports(code: string): string {
  return code.replace(/^\s*import\s+[^\n]*$/gm, '')
}


// A block is an ILLUSTRATION shape — documentation, not runnable code — if
// it matches this NARROW allowlist. These are the ONLY shapes a
// non-type-checking block is allowed to be; anything else that fails to
// type-check is a real bug and fails the gate. Three shapes:
//   1. a bare method / constructor signature with a typed-parameter
//      signature ("?:", e.g. `options?: { base?: string }`);
//   2. a placeholder-comment value ("/*", e.g. `field: /* string */`);
//   3. a type-SHAPE block that lists fields as `name: TypeKeyword` with the
//      type standing alone as the value — e.g. the DirectResult / FetchDef
//      shape `{ ok: boolean; status: number; ... }`. Those bare keywords
//      are undeclared VALUES (TS2693 "only refers to a type"). Matched
//      only at the START of a line, so a genuine annotation inside runnable
//      code (e.g. `(ctx: any) => ...`) is NOT matched.
const SHAPE_ILLUSTRATION =
  /^\s*[A-Za-z_$][\w$]*\??\s*:\s*(?:boolean|number|string|object|any|unknown|never|void|symbol|bigint|Record<|Partial<|Array<|Map<|Set<)/m
function isIllustrationShape(code: string): boolean {
  if (code.includes('?:')) {
    return true
  }
  if (code.includes('/*')) {
    return true
  }
  if (SHAPE_ILLUSTRATION.test(code)) {
    return true
  }
  return false
}


// A block is RUNNABLE if it constructs the SDK (`new <Sdk>SDK` /
// `<Sdk>SDK.test`) or drives a `client.` — those are the blocks the execute
// gate rewrites to seeded test mode and runs.
function isRunnable(code: string): boolean {
  return (
    /new\s+GitlabSDK\b/.test(code) ||
    /\bGitlabSDK\.test\b/.test(code) ||
    /\bclient\s*\./.test(code)
  )
}


// Wrap ONE block as a self-contained temp module: the block goes in its own
// scope inside an async function with a shared seeded `client`.
function buildSnippetModule(block: string): string {
  const inner = stripImports(block).split('\n').map((l) => '    ' + l).join('\n')
  return [
    "import { " + SDK_NAME + " } from '..'",
    '',
    'async function __ex() {',
    // Shared client for snippets that reference `client` without constructing
    // one (e.g. the direct() example).
    '  const client = ' + SDK_NAME + '.test()',
    '  void client',
    '  {',
    inner,
    '  }',
    '}',
    'void __ex',
    '',
  ].join('\n')
}


// Compile a batch of blocks in ONE tsc pass — one temp file per block, so
// each is an isolated program entry — and return which snippet indices
// errored. NOTE: tsc suppresses ALL semantic diagnostics program-wide as
// soon as any file has a SYNTAX error, so a single batch cannot be trusted;
// `typeCheckBlocks` drives this in a fixpoint that removes erroring blocks
// and recompiles the rest until a pass is clean.
function compileBatch(indices: number[], blocks: string[], key: string): {
  errored: Set<number>; raw: string; unattributed: boolean
} {
  const tsDir = Path.join(__dirname, '..')
  const testDir = Path.join(tsDir, 'test')
  // Leading dot: TypeScript wildcard includes skip dot-files, so these temp
  // files are never swept into the normal build.
  const files: string[] = []

  try {
    for (const i of indices) {
      const f = Path.join(testDir, '.examples_' + key + '_snippet' + i + '.gen.ts')
      Fs.writeFileSync(f, buildSnippetModule(blocks[i]), 'utf8')
      files.push(f)
    }

    const requireFrom = createRequire(__filename)
    const tsc = requireFrom.resolve('typescript/bin/tsc')

    const res = spawnSync(process.execPath, [
      tsc,
      '--noEmit',
      '--strict',
      '--pretty', 'false',
      '--target', 'ES2022',
      '--module', 'nodenext',
      '--moduleResolution', 'nodenext',
      '--esModuleInterop',
      '--resolveJsonModule',
      '--skipLibCheck',
      '--types', 'node',
      ...files,
    ], { cwd: tsDir, encoding: 'utf8' })

    const raw = ((res.stdout || '') + (res.stderr || '')).trim()
    const errored = new Set<number>()
    let unattributed = false

    if (0 !== res.status) {
      for (const line of raw.split('\n')) {
        const m = /_snippet(\d+)\.gen\.ts\(\d+,\d+\):\s*error/.exec(line)
        if (null == m) {
          // A genuine error diagnostic not attributable to a snippet file
          // (e.g. inside the SDK itself) is an anomaly.
          if (/:\s*error\s+TS\d+/.test(line)) {
            unattributed = true
          }
          continue
        }
        errored.add(parseInt(m[1], 10))
      }
    }

    return { errored, raw, unattributed }
  } finally {
    for (const f of files) {
      Fs.rmSync(f, { force: true })
    }
  }
}


// Determine, robustly, which of a doc's blocks fail to type-check. Because
// one syntax error suppresses semantic diagnostics for the whole batch, we
// iterate: compile the still-unresolved blocks, record the ones that
// errored, drop them, and recompile the rest — until a pass reports no
// errors. Each erroring pass strictly shrinks the working set, so this
// terminates. `globalError` flags an unattributable tsc error, surfaced
// verbatim by the caller.
function typeCheckBlocks(blocks: string[], key: string): {
  failing: Set<number>; raw: string; globalError: boolean
} {
  const failing = new Set<number>()
  const raws: string[] = []
  let globalError = false
  let remaining = blocks.map((_b, i) => i)

  // Guard: at most one pass per block plus a final clean pass.
  for (let pass = 0; pass <= blocks.length && 0 < remaining.length; pass++) {
    const { errored, raw, unattributed } = compileBatch(remaining, blocks, key)
    raws.push(raw)
    if (unattributed) {
      globalError = true
    }
    if (0 === errored.size) {
      break
    }
    for (const i of errored) {
      failing.add(i)
    }
    remaining = remaining.filter((i) => !errored.has(i))
  }

  return { failing, raw: raws.filter(Boolean).join('\n'), globalError }
}


// Turn a documented snippet into an offline-runnable one: strip imports,
// rewrite real client construction (`new <Sdk>SDK(...)` and any
// `<Sdk>SDK.test(...)`) to a seeded test-mode client, and give snippets
// that reference `client` without constructing one a shared seeded client.
function rewriteForRun(code: string): string {
  let out = stripImports(code)
  out = out.replace(/new\s+GitlabSDK\s*\([^)]*\)/g, () => SEEDED_CTOR)
  out = out.replace(/GitlabSDK\.test\s*\([^)]*\)/g, () => SEEDED_CTOR)
  if (/\bclient\b/.test(out) && !/\b(?:const|let|var)\s+client\b/.test(out)) {
    out = 'const client = ' + SEEDED_CTOR + '\n' + out
  }
  return out
}


// Run every runnable, non-illustration block from a doc offline in seeded
// test mode. Returns a list of failure descriptions (empty === all passed).
async function executeBlocks(blocks: string[]): Promise<string[]> {
  const requireFrom = createRequire(__filename)
  const ts = requireFrom('typescript')

  const AsyncFunction = Object.getPrototypeOf(async function () {}).constructor
  const silentConsole = { log() {}, error() {}, warn() {}, info() {} }

  const failures: string[] = []

  for (let i = 0; i < blocks.length; i++) {
    // Illustration shapes are documentation, not runnable code.
    if (!isRunnable(blocks[i]) || isIllustrationShape(blocks[i])) {
      continue
    }

    const src = rewriteForRun(blocks[i])
    if ('' === src.trim()) {
      continue
    }

    // Compile the snippet to JS (strips type annotations) so it runs under
    // node exactly as a real caller would.
    const js = ts.transpileModule(src, {
      compilerOptions: {
        target: ts.ScriptTarget.ES2020,
        module: ts.ModuleKind.ESNext,
      },
    }).outputText

    let runner: any
    try {
      runner = new AsyncFunction(SDK_NAME, 'console', js)
    } catch (err: any) {
      failures.push('snippet ' + i + ' failed to compile as runnable JS: ' +
        String((err && err.message) || err))
      continue
    }

    try {
      await runner(GitlabSDK, silentConsole)
    } catch (err: any) {
      const msg = String((err && err.message) || err)
      // Tolerate the one expected domain error: no fixture seeded for the
      // requested id (a 404 / "Not found" from test mode). A programming
      // error (undefined method/symbol, wrong arg count) is a real doc bug
      // and is NOT tolerated.
      if (/\b(?:404|not found)\b/i.test(msg)) {
        continue
      }
      failures.push('snippet ' + i + ' threw at runtime: ' + msg)
    }
  }

  return failures
}


function indentBlock(code: string): string {
  return code.replace(/^/gm, '    ').replace(/\s+$/, '')
}


describe('README examples', () => {
  for (const doc of DOCS) {
    it(doc.label + ' — every example type-checks or is a known illustration', () => {
      // A target without this doc has nothing to check.
      if (!Fs.existsSync(doc.path)) {
        return
      }
      const md = Fs.readFileSync(doc.path, 'utf8')
      const blocks = extractTsBlocks(md)
      assert(blocks.length > 0, 'No TypeScript code blocks found in ' + doc.label)

      const { failing, raw, globalError } = typeCheckBlocks(blocks, doc.key)
      assert(!globalError,
        'tsc reported an error outside any snippet in ' + doc.label +
        ' — the harness could not attribute it:\n\n' + raw + '\n')

      let executed = 0
      let typecheckedOnly = 0
      let illustration = 0
      const unrecognized: string[] = []

      blocks.forEach((b, i) => {
        const typechecks = !failing.has(i)
        const shape = isIllustrationShape(b)
        const runnable = isRunnable(b) && !shape

        if (!typechecks && shape) {
          illustration++
        } else if (!typechecks && !shape) {
          unrecognized.push('  snippet ' + i + ':\n' + indentBlock(b))
        } else if (typechecks && runnable) {
          executed++
        } else {
          typecheckedOnly++
        }
      })

      // Emit the coverage summary FIRST so it is always visible, even when
      // an assertion below fails.
      console.log('README examples [' + doc.label + ']: total=' + blocks.length +
        ' executed=' + executed + ' typechecked=' + typecheckedOnly +
        ' illustration=' + illustration)

      assert.strictEqual(unrecognized.length, 0,
        'These ' + doc.label + ' ```ts blocks neither type-check nor match the ' +
        'illustration allowlist. Fix the snippet (or the SDK), or — if it is ' +
        'genuinely illustrative — reshape it to the allowlist:\n\n' +
        unrecognized.join('\n\n') + '\n\ntsc output:\n' + raw + '\n')

      assert.strictEqual(blocks.length, executed + typecheckedOnly + illustration,
        'completeness partition mismatch for ' + doc.label + ': ' + blocks.length +
        ' total != ' + executed + ' executed + ' + typecheckedOnly +
        ' typechecked + ' + illustration + ' illustration')
    })

    it(doc.label + ' — every runnable example executes in test mode', async () => {
      if (!Fs.existsSync(doc.path)) {
        return
      }
      const md = Fs.readFileSync(doc.path, 'utf8')
      const blocks = extractTsBlocks(md)
      assert(blocks.length > 0, 'No TypeScript code blocks found in ' + doc.label)

      const failures = await executeBlocks(blocks)
      assert.strictEqual(failures.length, 0,
        doc.label + ' runnable TypeScript snippets failed to run in test mode:\n\n' +
        failures.join('\n') + '\n')
    })
  }
})
