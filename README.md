# Gitlab SDK

GitLab API client, generated from the OpenAPI spec.

Learn more about GitLab API at [gitlab.com](https://gitlab.com).

This is an unofficial SDK for the GitLab public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

## Entities, not endpoints

This SDK exposes the API as a small set of **semantic entities** — ApiEntitiesProjectWithAccess and Project — that you
call directly, instead of assembling URL paths and query strings. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`):

```ts
const client = new GitlabSDK()
const apientitiesprojectwithaccess = await client.ApiEntitiesProjectWithAccess().load({ id: "example_id" })
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = GitlabSDK.test({
  entity: {
    api_entities_project_with_access: {
      test01: { id: 'test01' },
    },
  },
})
const apientitiesprojectwithaccess = await client.ApiEntitiesProjectWithAccess().load({ id: 'test01' })
// apientitiesprojectwithaccess is the ApiEntitiesProjectWithAccess entity, populated with mock data
// — call apientitiesprojectwithaccess.data() for the record itself
console.log(apientitiesprojectwithaccess)
```

### Python

```python
client = GitlabSDK.test()
apientitiesprojectwithaccess = client.ApiEntitiesProjectWithAccess().load({"id": "test01"})
print(apientitiesprojectwithaccess)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = GitlabSDK::test([
    "entity" => ["apientitiesprojectwithaccess" => ["test01" => ["id" => "test01"]]],
]);
$apientitiesprojectwithaccess = $client->ApiEntitiesProjectWithAccess()->load(["id" => "test01"]);
```

### Golang

```go
client := sdk.Test()
result, err := client.ApiEntitiesProjectWithAccess(nil).Load(
    map[string]any{"id": "test01"}, nil,
)
```

### Lua

```lua
local client = sdk.test()
local result, err = client:ApiEntitiesProjectWithAccess():load({ id = "test01" })
```

### JavaScript

```js
const client = GitlabSDK.test()
const apientitiesprojectwithaccess = await client.ApiEntitiesProjectWithAccess().load({ id: 'test01' })
// apientitiesprojectwithaccess is the entity, populated with mock data
// — call apientitiesprojectwithaccess.data() for the record itself
console.log(apientitiesprojectwithaccess)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/gitlab` | publish pending — [install from git tag](https://github.com/voxgig-sdk/gitlab-sdk/releases) |
| Python | `voxgig-sdk-gitlab` | publish pending — [install from git tag](https://github.com/voxgig-sdk/gitlab-sdk/releases) |
| PHP | `voxgig-sdk/gitlab` | publish pending — [install from git tag](https://github.com/voxgig-sdk/gitlab-sdk/releases) |
| Golang | `github.com/voxgig-sdk/gitlab-sdk/go` | `go get github.com/voxgig-sdk/gitlab-sdk/go@latest` |
| Lua | `voxgig-sdk-gitlab` | publish pending — [install from git tag](https://github.com/voxgig-sdk/gitlab-sdk/releases) |
| JavaScript | `@voxgig-sdk/gitlab-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/gitlab-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/gitlab-sdk/go-cli` | `go install github.com/voxgig-sdk/gitlab-sdk/go-cli/cmd/gitlab@latest` |
| Go MCP server | `github.com/voxgig-sdk/gitlab-sdk/go-mcp` | `go get github.com/voxgig-sdk/gitlab-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { GitlabSDK } from '@voxgig-sdk/gitlab'

const client = new GitlabSDK({
  apikey: process.env.GITLAB_APIKEY,
})

// Load apientitiesprojectwithaccess data (returns a ApiEntitiesProjectWithAccess)
const apientitiesprojectwithaccess = await client.ApiEntitiesProjectWithAccess().load()
console.log(apientitiesprojectwithaccess)
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o gitlab-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "gitlab": {
      "command": "/abs/path/to/gitlab-mcp"
    }
  }
}
```

## Entities

The API exposes 2 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **ApiEntitiesProjectWithAccess** | The ApiEntitiesProjectWithAccess entity (load). | `/api/v4/projects/{id}` |
| **Project** | The Project entity (create, list, remove, update). | `/api/v4/projects` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from gitlab_sdk import GitlabSDK

client = GitlabSDK({
    "apikey": os.environ.get("GITLAB_APIKEY"),
})


# Load a specific apientitiesprojectwithaccess (returns the record, raises on error)
apientitiesprojectwithaccess = client.ApiEntitiesProjectWithAccess().load({"id": "example_id"})
print(apientitiesprojectwithaccess)
```

### PHP

```php
<?php
require_once 'gitlab_sdk.php';

$client = new GitlabSDK([
    "apikey" => getenv("GITLAB_APIKEY"),
]);


// Load a specific apientitiesprojectwithaccess (returns the ENTITY; call data_get() for the record; throws on error)
$apientitiesprojectwithaccess = $client->ApiEntitiesProjectWithAccess()->load(["id" => "example_id"]);
print_r($apientitiesprojectwithaccess);
```

### Golang

```go
import sdk "github.com/voxgig-sdk/gitlab-sdk/go"

client := sdk.NewGitlabSDK(map[string]any{
    "apikey": os.Getenv("GITLAB_APIKEY"),
})

// Load apientitiesprojectwithaccess data
apiEntitiesProjectWithAccess, err := client.ApiEntitiesProjectWithAccess(nil).Load(map[string]any{"id": "example_id"}, nil)
if err != nil {
    panic(err)
}
fmt.Println(apiEntitiesProjectWithAccess)
```

### Lua

```lua
local sdk = require("gitlab_sdk")

local client = sdk.new({
  apikey = os.getenv("GITLAB_APIKEY"),
})


-- Load a specific apientitiesprojectwithaccess
local apientitiesprojectwithaccess, err = client:ApiEntitiesProjectWithAccess():load({ id = "example_id" })
print(apientitiesprojectwithaccess)
```

### JavaScript

```js
const { GitlabSDK } = require('@voxgig-sdk/gitlab-js')

const client = new GitlabSDK({
  apikey: process.env.GITLAB_APIKEY,
})

```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://gitlab.com](https://gitlab.com)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the GitLab API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
